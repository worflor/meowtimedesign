name: distribution-stable

on:
  workflow_call:
    inputs:
      prerelease_version:
        description: "Exact validated prerelease version to promote."
        required: true
        type: string
      publish:
        description: "Publish immutable stable version objects. False seals an unlisted candidate."
        required: false
        type: boolean
        default: false
      promote:
        description: "Activate stable/latest and expose the GitHub Release after publication."
        required: false
        type: boolean
        default: false
      ref:
        description: "Stable release branch to build. Empty builds the caller's ref, which must also be release/vX.Y.Z."
        required: false
        type: string
        default: ""
      mac_arm64_sign_mode:
        description: "macOS arm64 signing mode: unsigned, signed, or notarized."
        required: false
        type: string
        default: "notarized"
      mac_x64_sign_mode:
        description: "macOS x64 signing mode: unsigned, signed, or notarized."
        required: false
        type: string
        default: "notarized"
      win_x64_sign_mode:
        description: "Windows x64 signing mode: unsigned or signed."
        required: false
        type: string
        default: "unsigned"
      win_x64_smoke_mode:
        description: "Windows x64 smoke coverage: skip, core, or full."
        required: false
        type: string
        default: "core"
      win_x64_update_metadata_url:
        description: "Optional external updater metadata URL for full Windows x64 payload smoke."
        required: false
        type: string
        default: ""
      win_x64_update_target_version:
        description: "Optional external updater target version expected by full Windows x64 payload smoke."
        required: false
        type: string
        default: ""
    outputs:
      release_version:
        description: "The stable version that was validated or published."
        value: ${{ jobs.metadata.outputs.release_version }}
      candidate_id:
        description: "Immutable candidate build-spec digest."
        value: ${{ jobs.metadata.outputs.candidate_id }}
      candidate_manifest_url:
        description: "Public unlisted candidate manifest URL when publish is false."
        value: ${{ jobs.candidate.outputs.manifest_url }}
      channel:
        description: "The stable release channel."
        value: ${{ jobs.metadata.outputs.channel }}
      branch:
        description: "The branch that was built."
        value: ${{ jobs.metadata.outputs.branch }}
      commit:
        description: "The commit SHA that was built."
        value: ${{ jobs.metadata.outputs.commit }}
      dry_run:
        description: "Whether this run is a dry-run."
        value: ${{ jobs.metadata.outputs.dry_run }}
      dry_run_mode:
        description: "The stable dry-run boundary: metadata or prepublish."
        value: ${{ jobs.metadata.outputs.dry_run_mode }}
      previous_commit:
        description: "Commit SHA of the previously published prerelease (changelog baseline); empty on cold start."
        value: ${{ jobs.metadata.outputs.previous_commit }}
      version_metadata_url:
        description: "Public R2 URL of this build's version metadata.json."
        value: ${{ inputs.publish && jobs.publish.outputs.version_metadata_url || '' }}
      mac_arm64_url:
        description: "macOS arm64 dmg download URL."
        value: ${{ inputs.publish && jobs.publish.outputs.mac_arm64_url || jobs.candidate.outputs.mac_arm64_url }}
      mac_intel_url:
        description: "macOS x64 (Intel) dmg download URL."
        value: ${{ inputs.publish && jobs.publish.outputs.mac_intel_url || jobs.candidate.outputs.mac_x64_url }}
      win_url:
        description: "Windows x64 installer download URL."
        value: ${{ inputs.publish && jobs.publish.outputs.win_url || jobs.candidate.outputs.win_x64_url }}

permissions:
  actions: write
  contents: write
  id-token: write
  packages: write


env:
  RELEASE_MAC_ARM64_SIGN_MODE: ${{ inputs.mac_arm64_sign_mode }}
  RELEASE_MAC_X64_SIGN_MODE: ${{ inputs.mac_x64_sign_mode }}
  RELEASE_WIN_X64_SIGN_MODE: ${{ inputs.win_x64_sign_mode }}
  # Workspace Team's four vela transports are gated in the packaged runtime on
  # BOTH a known AMR profile and a non-empty vela web origin
  # (workspaceTeamTransportEnv in shells/electron/src/workspace-team.ts). Neither
  # has a default in tools-pack, so a lane that omits them ships a client whose
  # team features are silently dormant: the build succeeds, the app starts, and
  # the gap only shows up in a user's hands.
  #
  # Unlike beta/prerelease these are production channels, so the profile is
  # pinned rather than taken from an input — there is no legitimate stable or
  # preview build aimed at the test backend. Naming prod explicitly is a no-op
  # for AMR itself (apps/daemon/src/integrations/vela-profile.ts already
  # defaults to prod); it exists to satisfy the first half of the gate. A fork
  # or PR build without the secret leaves the origin empty, which keeps the
  # transports dormant rather than half-enabled.
  OPEN_DESIGN_AMR_PROFILE: prod
  OD_VELA_WEB_URL: ${{ secrets.VELA_WEB_URL_PROD }}
  OPEN_DESIGN_TELEMETRY_RELAY_URL: ${{ vars.OPEN_DESIGN_TELEMETRY_RELAY_URL }}
  # PostHog product-analytics ingest. Defined as repository secret + var
  # so official builds ship with analytics enabled; PR builds and forks
  # without these run the daemon in no-op analytics mode (events never
  # leave the user's machine, /api/analytics/config returns enabled=false).
  POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
  POSTHOG_HOST: ${{ vars.POSTHOG_HOST }}
  # PostHog Error tracking sourcemap upload. Personal API key (phx_...) and
  # project ID let tools-pack's web-sourcemaps step ship browser sourcemaps
  # to PostHog after `next build` and before the .map files are stripped
  # from the packaged bundle. Missing in PR/fork builds → upload is skipped
  # and the helper still strips .map to keep source out of the installer.
  POSTHOG_CLI_API_KEY: ${{ secrets.POSTHOG_CLI_API_KEY }}
  POSTHOG_CLI_PROJECT_ID: ${{ vars.POSTHOG_CLI_PROJECT_ID }}
  # Run/repo attribution for the published metadata + per-platform manifests,
  # set workflow-wide so the platform build jobs and the publish-metadata step
  # agree on github.runId — publish-metadata.ts refuses a platform manifest
  # whose github.{runId,commit} disagree with the publish step's. These are the
  # same for caller and callee under workflow_call, so they're safe here. Branch
  # is NOT: under workflow_call (or inputs.ref) github.ref_name is the caller's
  # ref, so RELEASE_BRANCH is passed per publish step as the resolved
  # needs.metadata.outputs.branch instead, matching what the metadata job built.
  RELEASE_REPOSITORY: ${{ github.repository }}
  RELEASE_RUN_ATTEMPT: ${{ github.run_attempt }}
  RELEASE_RUN_ID: ${{ github.run_id }}
  RELEASE_WORKFLOW: ${{ github.workflow }}

jobs:
  metadata:
    name: Prepare release metadata
    if: github.repository == 'nexu-io/open-design'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      OPEN_DESIGN_PRERELEASE_METADATA_URL: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}/prerelease/latest/metadata.json
      OPEN_DESIGN_RELEASE_DRY_RUN: ${{ inputs.publish && 'false' || 'prepublish' }}
      OPEN_DESIGN_RELEASE_CHANNEL: stable
      OPEN_DESIGN_RELEASES_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
      OPEN_DESIGN_STABLE_WIN_X64_SMOKE_MODE: ${{ inputs.win_x64_smoke_mode }}
      OPEN_DESIGN_STABLE_PRERELEASE_VERSION: ${{ inputs.prerelease_version }}
    outputs:
      base_version: ${{ steps.stable.outputs.base_version }}
      branch: ${{ steps.stable.outputs.branch }}
      channel: ${{ steps.stable.outputs.channel }}
      commit: ${{ steps.stable.outputs.commit }}
      candidate_id: ${{ steps.candidate.outputs.candidate_id }}
      dry_run: ${{ steps.stable.outputs.dry_run }}
      dry_run_mode: ${{ steps.stable.outputs.dry_run_mode }}
      github_release_enabled: ${{ steps.stable.outputs.github_release_enabled }}
      mac_intel_namespace: ${{ steps.stable.outputs.mac_intel_namespace }}
      namespace: ${{ steps.stable.outputs.namespace }}
      publish_side_effects_enabled: ${{ steps.stable.outputs.publish_side_effects_enabled }}
      prerelease_number: ${{ steps.stable.outputs.prerelease_number }}
      previous_stable: ${{ steps.stable.outputs.previous_stable }}
      previous_commit: ${{ steps.prev.outputs.previous_commit }}
      release_name: ${{ steps.stable.outputs.release_name }}
      release_version: ${{ steps.stable.outputs.release_version }}
      run_prepublish_jobs: ${{ steps.stable.outputs.run_prepublish_jobs }}
      stable_version: ${{ steps.stable.outputs.stable_version }}
      state_source: ${{ steps.stable.outputs.state_source }}
      version_tag: ${{ steps.stable.outputs.version_tag }}
      win_namespace: ${{ steps.stable.outputs.win_namespace }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref != '' && inputs.ref || github.ref }}
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.33.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.18.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Under workflow_call the runner's GITHUB_SHA/GITHUB_REF_NAME describe the
      # caller's ref (e.g. main for the scheduled daily build), not the ref we
      # actually checked out. Resolve the real built HEAD so the published
      # metadata.json records the branch we built, not the caller.
      - name: Resolve built commit
        run: echo "BUILT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"

      - name: Validate publication mode
        if: ${{ inputs.promote && !inputs.publish }}
        run: |
          echo "promote=true requires publish=true" >&2
          exit 1

      # Capture the previously published prerelease commit BEFORE this run overwrites
      # prerelease/latest/metadata.json, so the notification can diff the changelog
      # against the last package. Empty on cold start (HTTP 404).
      - name: Capture previous prerelease commit
        id: prev
        run: |
          set -euo pipefail
          prev="$(curl -fsSL "$OPEN_DESIGN_PRERELEASE_METADATA_URL" | jq -r '.github.commit // ""' 2>/dev/null || true)"
          echo "previous prerelease commit: ${prev:-<none>}"
          echo "previous_commit=$prev" >> "$GITHUB_OUTPUT"

      - name: Prepare release metadata
        id: stable
        env:
          GITHUB_SHA: ${{ env.BUILT_SHA }}
          GITHUB_REF_NAME: ${{ inputs.ref != '' && inputs.ref || github.ref_name }}
        run: pnpm exec tools-release prepare stable

      - name: Prepare immutable candidate identity
        id: candidate
        env:
          RELEASE_CANDIDATE_SPEC_PATH: ${{ runner.temp }}/release-candidate/spec.json
        run: |
          set -euo pipefail
          mkdir -p "$(dirname "$RELEASE_CANDIDATE_SPEC_PATH")"
          jq -n \
            --arg commit "$BUILT_SHA" \
            --arg releaseVersion "${{ steps.stable.outputs.release_version }}" \
            --arg macArm64SignMode "${{ inputs.mac_arm64_sign_mode }}" \
            --arg macX64SignMode "${{ inputs.mac_x64_sign_mode }}" \
            --arg winX64SignMode "${{ inputs.win_x64_sign_mode }}" \
            '{amrProfile: "prod", channel: "stable", closureMinShellVersion: "${{ vars.RELEASE_SHELL_VERSION_MIN_STABLE }}", commit: $commit, macArm64SignMode: $macArm64SignMode, macX64SignMode: $macX64SignMode, releaseVersion: $releaseVersion, schemaVersion: 1, targets: ["mac_arm64", "mac_x64", "win_x64"], winX64SignMode: $winX64SignMode}' > "$RELEASE_CANDIDATE_SPEC_PATH"
          pnpm exec tools-release prepare-candidate

      - name: Upload immutable candidate spec
        uses: actions/upload-artifact@v7
        with:
          name: open-design-stable-candidate-spec
          path: ${{ runner.temp }}/release-candidate/spec.json
          if-no-files-found: error
          retention-days: 30

      - name: Validate stable release note policy
        env:
          RELEASE_CHANNEL: stable
          RELEASE_NOTE_PLAN_PATH: ${{ runner.temp }}/release-note-preflight/plan.json
          RELEASE_VERSION: ${{ steps.stable.outputs.release_version }}
        run: pnpm exec tools-release prepare-release-note

      - name: Validate R2 release access
        env:
          GITHUB_SHA: ${{ env.BUILT_SHA }}
          R2_ACCESS_PROBE_NAME: ${{ steps.stable.outputs.namespace }}
          RELEASE_CHANNEL: stable
          RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
          RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
          RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
          RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
          RELEASE_STORAGE_REGION: auto
          RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
        run: pnpm exec tools-release check-storage

  closure_shared:
    name: Build stable shared Standalone Closure
    needs: metadata
    if: ${{ needs.metadata.outputs.run_prepublish_jobs == 'true' }}
    runs-on: ubuntu-latest
    env:
      RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
      RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
      RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
      RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
      RELEASE_STORAGE_REGION: auto
      RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ needs.metadata.outputs.commit }}
      - name: Setup release toolchain
        uses: ./.github/actions/release/platform/setup
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Resolve shared Closure identity
        id: closure_shared_identity
        run: |
          set -euo pipefail
          digest="$(pnpm exec tools-release identity digest closure.shared.build \
            --parameter minShellVersion=${{ vars.RELEASE_SHELL_VERSION_MIN_STABLE }} \
            --parameter "nodeVersion=$(node -p 'process.version')" \
            --parameter "packageManager=$(node -p 'require("./package.json").packageManager')")"
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      - name: Build shared Standalone Closure
        uses: ./.github/actions/release/closure/shared
        with:
          channel: stable
          identity-digest: ${{ steps.closure_shared_identity.outputs.digest }}
          min-shell-version: ${{ vars.RELEASE_SHELL_VERSION_MIN_STABLE }}
          publish-enabled: "true"
          release-version: ${{ needs.metadata.outputs.release_version }}

  verify:
    name: Verify build (typecheck + tests)
    needs: metadata
    if: ${{ needs.metadata.outputs.run_prepublish_jobs == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.33.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.18.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # `scripts/postinstall.mjs` auto-builds `packages/*` and `tools/*`, but
      # `apps/daemon` and `shells/electron` are not in that list. On a fresh clone
      # (every CI run), workspace typecheck fails because:
      #   - packaged/runtime consumers resolve the daemon package export through
      #     generated `apps/daemon/dist/*.d.ts`
      #   - `shells/electron/src/index.ts` dynamic-imports `@open-design/shell-electron/main`
      #     which resolves to `shells/electron/dist/main/index.d.ts`
      # Build them explicitly here. Keeps the root `typecheck` script untouched.
      - name: Build daemon and desktop (typecheck dependencies)
        run: |
          pnpm --filter @open-design/daemon build
          pnpm --filter @open-design/shell-electron build

      - name: Typecheck workspaces
        run: pnpm -r --workspace-concurrency=4 --if-present run typecheck

      - name: Check repository layout policies
        run: pnpm guard

      # Workspace tests are intentionally not gated here. apps/web's
      # i18n content-coverage tests assert that every locale carries
      # display metadata for every prompt template / skill / design
      # system. Those tests fail on `main` as of this writing because
      # PR #187 added two new prompt templates without translating
      # their metadata into the 9 ship-ready locales — an i18n drift
      # that's out of scope for the release infrastructure. Tracked as
      # a follow-up; revisit once locale metadata is back in sync.

  build_mac:
    name: Build release mac arm64
    needs: [metadata, closure_shared, verify]
    if: ${{ needs.metadata.outputs.run_prepublish_jobs == 'true' }}
    runs-on: macos-14
    outputs:
      candidate_manifest_url: ${{ steps.platform.outputs.candidate_manifest_url }}
      candidate_primary_url: ${{ steps.platform.outputs.candidate_primary_url }}
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
      APPLE_SIGNING_CERTIFICATE_BASE64: ${{ secrets.APPLE_SIGNING_CERTIFICATE_BASE64 }}
      APPLE_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_SIGNING_CERTIFICATE_PASSWORD }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      GH_TOKEN: ${{ github.token }}
      RELEASE_BRANCH: ${{ needs.metadata.outputs.branch }}
      RELEASE_COMMIT: ${{ needs.metadata.outputs.commit }}
      RELEASE_DRY_RUN_MODE: ${{ needs.metadata.outputs.dry_run_mode }}
      RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
      RELEASE_PUBLISH_SIDE_EFFECTS: "true"
      RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
      RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
      RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
      RELEASE_STORAGE_REGION: auto
      RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
      - name: Distribute stable mac_arm64
        id: platform
        uses: ./.github/actions/release/platform/mac
        with:
          app-name: Open Design.app
          artifact-id: mac-arm64
          bundle-artifact-name: open-design-release-mac-release-assets
          cache-enabled: "true"
          channel: stable
          candidate-id: ${{ needs.metadata.outputs.candidate_id }}
          candidate-enabled: "true"
          closure-version: ${{ needs.metadata.outputs.release_version }}
          diagnostics: "true"
          diagnostics-artifact-name: open-design-release-mac-arm64-build-diagnostics
          inspect-framework: "true"
          manifest-artifact-name: open-design-release-mac-arm64-publish-manifest
          namespace: ${{ needs.metadata.outputs.namespace }}
          release-name: Open Design
          release-version: ${{ needs.metadata.outputs.release_version }}
          report-artifact-name: open-design-release-mac-e2e-report
          sign-mode: ${{ inputs.mac_arm64_sign_mode }}
          smoke-enabled: "true"
          target: mac_arm64
          upload-report: "true"
          verify-notarization: "true"
          write-report: "false"

  build_mac_intel:
    name: Build release mac intel x64
    needs: [metadata, closure_shared, verify]
    if: ${{ needs.metadata.outputs.run_prepublish_jobs == 'true' }}
    runs-on: macos-15-intel
    outputs:
      candidate_manifest_url: ${{ steps.platform.outputs.candidate_manifest_url }}
      candidate_primary_url: ${{ steps.platform.outputs.candidate_primary_url }}
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
      APPLE_SIGNING_CERTIFICATE_BASE64: ${{ secrets.APPLE_SIGNING_CERTIFICATE_BASE64 }}
      APPLE_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_SIGNING_CERTIFICATE_PASSWORD }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      GH_TOKEN: ${{ github.token }}
      RELEASE_BRANCH: ${{ needs.metadata.outputs.branch }}
      RELEASE_COMMIT: ${{ needs.metadata.outputs.commit }}
      RELEASE_DRY_RUN_MODE: ${{ needs.metadata.outputs.dry_run_mode }}
      RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
      RELEASE_PUBLISH_SIDE_EFFECTS: "true"
      RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
      RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
      RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
      RELEASE_STORAGE_REGION: auto
      RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
      - name: Distribute stable mac_x64
        id: platform
        uses: ./.github/actions/release/platform/mac
        with:
          app-name: Open Design.app
          artifact-id: mac-x64
          bundle-artifact-name: open-design-release-mac-intel-release-assets
          cache-enabled: "true"
          channel: stable
          candidate-id: ${{ needs.metadata.outputs.candidate_id }}
          candidate-enabled: "true"
          closure-version: ${{ needs.metadata.outputs.release_version }}
          manifest-artifact-name: open-design-release-mac-x64-publish-manifest
          namespace: ${{ needs.metadata.outputs.mac_intel_namespace }}
          release-name: Open Design
          release-version: ${{ needs.metadata.outputs.release_version }}
          sign-mode: ${{ inputs.mac_x64_sign_mode }}
          smoke-enabled: "false"
          target: mac_x64
          verify-notarization: "true"
          write-report: "false"

  build_win:
    name: Build release win x64
    needs: [metadata, closure_shared, verify]
    if: ${{ needs.metadata.outputs.run_prepublish_jobs == 'true' }}
    runs-on: windows-latest
    outputs:
      candidate_manifest_url: ${{ steps.platform.outputs.candidate_manifest_url }}
      candidate_primary_url: ${{ steps.platform.outputs.candidate_primary_url }}
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_BRANCH: ${{ needs.metadata.outputs.branch }}
      RELEASE_COMMIT: ${{ needs.metadata.outputs.commit }}
      RELEASE_DRY_RUN_MODE: ${{ needs.metadata.outputs.dry_run_mode }}
      RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
      RELEASE_PUBLISH_SIDE_EFFECTS: "true"
      RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
      RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
      RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
      RELEASE_STORAGE_REGION: auto
      RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
      - name: Distribute stable win_x64
        id: platform
        uses: ./.github/actions/release/platform/win
        with:
          bundle-artifact-name: open-design-release-win-release-assets
          channel: stable
          candidate-id: ${{ needs.metadata.outputs.candidate_id }}
          candidate-enabled: "true"
          closure-version: ${{ needs.metadata.outputs.release_version }}
          manifest-artifact-name: open-design-release-win-x64-publish-manifest
          namespace: ${{ needs.metadata.outputs.win_namespace }}
          release-name: Open Design
          release-version: ${{ needs.metadata.outputs.release_version }}
          report-artifact-name: open-design-release-win-e2e-report
          sign-mode: ${{ inputs.win_x64_sign_mode }}
          smoke-mode: ${{ inputs.win_x64_smoke_mode }}
          update-metadata-url: ${{ inputs.win_x64_update_metadata_url }}
          update-target-version: ${{ inputs.win_x64_update_target_version }}
          update-version-policy: patch
          write-report: "false"

  candidate:
    name: Seal stable candidate
    needs: [metadata, build_mac, build_mac_intel, build_win, publish]
    if: ${{ !cancelled() && !inputs.promote && needs.metadata.result == 'success' && needs.build_mac.result == 'success' && needs.build_mac_intel.result == 'success' && needs.build_win.result == 'success' && needs.publish.result == 'success' }}
    runs-on: ubuntu-latest
    outputs:
      manifest_url: ${{ steps.finalize.outputs.candidate_manifest_url }}
      mac_arm64_url: ${{ needs.build_mac.outputs.candidate_primary_url }}
      mac_x64_url: ${{ needs.build_mac_intel.outputs.candidate_primary_url }}
      win_x64_url: ${{ needs.build_win.outputs.candidate_primary_url }}
    steps:
      - name: Checkout candidate finalizer
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ needs.metadata.outputs.commit }}
      - name: Setup release toolchain
        uses: ./.github/actions/release/platform/setup
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Download candidate spec
        uses: actions/download-artifact@v8
        with:
          name: open-design-stable-candidate-spec
          path: ${{ runner.temp }}/release-candidate
      - name: Prepare candidate target URLs
        env:
          MAC_ARM64_MANIFEST: ${{ needs.build_mac.outputs.candidate_manifest_url }}
          MAC_X64_MANIFEST: ${{ needs.build_mac_intel.outputs.candidate_manifest_url }}
          WIN_X64_MANIFEST: ${{ needs.build_win.outputs.candidate_manifest_url }}
        run: jq -n '[env.MAC_ARM64_MANIFEST, env.MAC_X64_MANIFEST, env.WIN_X64_MANIFEST]' > "$RUNNER_TEMP/release-candidate/target-urls.json"
      - name: Seal candidate manifest
        id: finalize
        env:
          RELEASE_CANDIDATE_SPEC_PATH: ${{ runner.temp }}/release-candidate/spec.json
          RELEASE_CANDIDATE_TARGET_URLS_PATH: ${{ runner.temp }}/release-candidate/target-urls.json
          RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
          RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
          RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
          RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
          RELEASE_STORAGE_REGION: auto
          RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
          RELEASE_VERSION_METADATA_URL: ${{ needs.publish.outputs.version_metadata_url }}
        run: pnpm exec tools-release finalize-candidate

  publish:
    name: Finalize ${{ needs.metadata.outputs.channel }} release plan
    needs:
      - metadata
      - closure_shared
      - verify
      - build_mac
      - build_mac_intel
      - build_win
    if: >-
      ${{
        always() &&
        !cancelled() &&
        needs.metadata.outputs.run_prepublish_jobs == 'true' &&
        needs.metadata.result == 'success' &&
        needs.verify.result == 'success' &&
        needs.build_mac.result == 'success' &&
        needs.build_mac_intel.result == 'success' &&
        needs.build_win.result == 'success'
      }}
    runs-on: ubuntu-latest
    outputs:
      version_metadata_url: ${{ steps.distribute.outputs.version_metadata_url }}
      mac_arm64_url: ${{ steps.distribute.outputs.mac_arm64_url }}
      mac_intel_url: ${{ steps.distribute.outputs.mac_x64_url }}
      win_url: ${{ steps.distribute.outputs.win_x64_url }}
    env:
      GH_TOKEN: ${{ github.token }}
      # Record the real built commit in metadata.json. Under workflow_call the
      # runner's GITHUB_SHA is the caller's ref, so pin it to the resolved build
      # commit; the next run reads this back as its changelog baseline.
      GITHUB_SHA: ${{ needs.metadata.outputs.commit }}
      BASE_VERSION: ${{ needs.metadata.outputs.base_version }}
      BRANCH_NAME: ${{ needs.metadata.outputs.branch }}
      ENABLE_MAC: "true"
      ENABLE_MAC_INTEL: "true"
      ENABLE_WIN: "true"
      GITHUB_RELEASE_ENABLED: ${{ needs.metadata.outputs.github_release_enabled }}
      PRERELEASE_NUMBER: ${{ needs.metadata.outputs.prerelease_number }}
      RELEASE_CHANNEL: ${{ needs.metadata.outputs.channel }}
      RELEASE_NAME: ${{ needs.metadata.outputs.release_name }}
      RELEASE_VERSION: ${{ needs.metadata.outputs.release_version }}
      REPORT_MODE: zip
      STATE_SOURCE: ${{ needs.metadata.outputs.state_source }}
      VERSION_TAG: ${{ needs.metadata.outputs.version_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.18.0

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.33.2

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download mac publish manifest
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-mac-arm64-publish-manifest
          path: ${{ runner.temp }}/release-platform-manifests

      - name: Download mac intel publish manifest
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-mac-x64-publish-manifest
          path: ${{ runner.temp }}/release-platform-manifests

      - name: Download windows publish manifest
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-win-x64-publish-manifest
          path: ${{ runner.temp }}/release-platform-manifests

      - name: Download shared Standalone contribution
        uses: actions/download-artifact@v8
        with:
          name: open-design-stable-closure-shared
          path: ${{ runner.temp }}/release-closure/shared

      - name: Download macOS arm64 Standalone contribution
        uses: actions/download-artifact@v8
        with:
          name: open-design-stable-closure-darwin-arm64
          path: ${{ runner.temp }}/release-closure/targets/darwin-arm64

      - name: Download macOS x64 Standalone contribution
        uses: actions/download-artifact@v8
        with:
          name: open-design-stable-closure-darwin-x64
          path: ${{ runner.temp }}/release-closure/targets/darwin-x64

      - name: Download Windows x64 Standalone contribution
        uses: actions/download-artifact@v8
        with:
          name: open-design-stable-closure-win32-x64
          path: ${{ runner.temp }}/release-closure/targets/win32-x64

      - name: Merge version-wide Standalone graph
        run: |
          set -euo pipefail
          pnpm exec tools-release merge-closure-distribution \
            "$RUNNER_TEMP/release-closure/shared/shared-contribution.json" \
            "$RUNNER_TEMP/release-closure/targets/darwin-arm64/target-contribution.json" \
            "$RUNNER_TEMP/release-closure/targets/darwin-x64/target-contribution.json" \
            "$RUNNER_TEMP/release-closure/targets/win32-x64/target-contribution.json" \
            --output "$RUNNER_TEMP/release-closure/closure-distribution.json"

      - name: Pre-flight tag/release check
        if: ${{ needs.metadata.outputs.github_release_enabled == 'true' }}
        run: |
          set -euo pipefail
          if git ls-remote --exit-code --tags origin "refs/tags/$VERSION_TAG" >/dev/null 2>&1; then
            echo "tag $VERSION_TAG already exists on origin; aborting" >&2
            exit 1
          fi
          if gh release view "$VERSION_TAG" >/dev/null 2>&1; then
            echo "release $VERSION_TAG already exists; aborting" >&2
            exit 1
          fi

      - name: Download mac release bundle
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-mac-release-assets
          path: ${{ runner.temp }}/release-assets/mac

      - name: Download mac intel release bundle
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-mac-intel-release-assets
          path: ${{ runner.temp }}/release-assets/mac-intel

      - name: Download windows release bundle
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-win-release-assets
          path: ${{ runner.temp }}/release-assets/win

      - name: Download mac e2e spec report
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-mac-e2e-report
          path: ${{ runner.temp }}/release-report/mac

      # The windows smoke uploads this artifact with `if-no-files-found: warn`,
      # so a skipped smoke leaves an empty report directory and publishes no
      # artifact at all. Downloading it unconditionally then fails finalize —
      # after all three platforms have already built, signed and notarized —
      # which makes `win_x64_smoke_mode: skip` unusable on the stable lane even
      # though the input offers it. Guard it by the requested smoke mode.
      - name: Download windows e2e spec report
        if: ${{ inputs.win_x64_smoke_mode != 'skip' }}
        uses: actions/download-artifact@v8
        with:
          name: open-design-release-win-e2e-report
          path: ${{ runner.temp }}/release-report/win

      - name: Prepare stable GitHub Release asset plan
        env:
          RELEASE_CHANNEL: ${{ needs.metadata.outputs.channel }}
          RELEASE_GITHUB_ASSETS_DIR: ${{ runner.temp }}/github-release-assets
          RELEASE_GITHUB_ASSETS_SOURCE_DIR: ${{ runner.temp }}/release-assets
          RELEASE_OUTPUTS_PATH: ${{ runner.temp }}/github-release-assets-plan/outputs.json
          RELEASE_SUMMARY_PATH: ${{ runner.temp }}/github-release-assets-plan/summary.md
          RELEASE_VERSION: ${{ needs.metadata.outputs.release_version }}
        run: pnpm exec tools-release prepare-github-assets

      - name: Publish GitHub Release asset plan summary
        run: cat "${{ runner.temp }}/github-release-assets-plan/summary.md" >> "$GITHUB_STEP_SUMMARY"

      - name: Write stable release notes
        id: notes
        if: ${{ needs.metadata.outputs.github_release_enabled == 'true' }}
        env:
          RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
        run: bash .github/scripts/release/github/stable-notes.sh

      - name: Create draft release with tag
        id: create_release
        if: ${{ needs.metadata.outputs.github_release_enabled == 'true' }}
        run: |
          set -euo pipefail
          # gh release create creates the tag at $GITHUB_SHA atomically with the release.
          # Using --draft keeps the release invisible until R2 publish and asset upload verify successfully.
          gh release create "$VERSION_TAG" \
            --target "$GITHUB_SHA" \
            --title "$RELEASE_NAME" \
            --notes-file "${{ steps.notes.outputs.notes_file }}" \
            --draft

      - name: Upload assets to draft release
        if: ${{ needs.metadata.outputs.github_release_enabled == 'true' }}
        run: |
          set -euo pipefail
          gh release upload "$VERSION_TAG" "$RUNNER_TEMP/github-release-assets"/*

      - name: Distribute stable release metadata
        id: distribute
        uses: ./.github/actions/release/publish-metadata
        with:
          enable_mac_arm64: "true"
          enable_mac_x64: "true"
          enable_win_x64: "true"
          mac_arm64_result: ${{ needs.build_mac.result }}
          mac_x64_result: ${{ needs.build_mac_intel.result }}
          win_x64_result: ${{ needs.build_win.result }}
        env:
          BASE_VERSION: ${{ needs.metadata.outputs.base_version }}
          RELEASE_ACTIVATE_LATEST: "false"
          RELEASE_ASSET_SUFFIX: ""
          RELEASE_BRANCH: ${{ needs.metadata.outputs.branch }}
          RELEASE_CHANNEL: ${{ needs.metadata.outputs.channel }}
          RELEASE_COMMIT: ${{ needs.metadata.outputs.commit }}
          RELEASE_CLOSURE_DISTRIBUTION_MANIFEST_PATH: ${{ runner.temp }}/release-closure/closure-distribution.json
          RELEASE_CLOSURE_DISTRIBUTION_REQUIRED: "true"
          RELEASE_CLOSURE_REQUIRED: "false"
          RELEASE_DRY_RUN_MODE: ${{ needs.metadata.outputs.dry_run_mode }}
          RELEASE_INSTALLATION_VERSION_MIN_STABLE: ${{ vars.RELEASE_LAUNCHER_VERSION_MIN_STABLE }}
          RELEASE_INSTALLATION_VERSION_MIN_URL_STABLE: ${{ vars.RELEASE_LAUNCHER_VERSION_MIN_URL_STABLE }}
          RELEASE_LEGACY_INSTALLATION_MIGRATION_REQUIRED: "true"
          RELEASE_PUBLISH_SIDE_EFFECTS: "true"
          RELEASE_MANIFEST_DIR: ${{ runner.temp }}/release-platform-manifests
          RELEASE_METADATA_PATH: ""
          RELEASE_PUBLIC_ORIGIN: ${{ vars.CLOUDFLARE_R2_RELEASES_PUBLIC_ORIGIN }}
          RELEASE_REPOSITORY: ${{ github.repository }}
          RELEASE_RUN_ATTEMPT: ${{ github.run_attempt }}
          RELEASE_RUN_ID: ${{ github.run_id }}
          RELEASE_SHELL_REQUIRED: "true"
          RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
          RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
          RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
          RELEASE_STORAGE_REGION: auto
          RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
          RELEASE_VERSION: ${{ needs.metadata.outputs.release_version }}
          RELEASE_WORKFLOW: ${{ github.workflow }}
          STABLE_VERSION: ${{ needs.metadata.outputs.stable_version }}
          STATE_SOURCE: ${{ needs.metadata.outputs.state_source }}
          VERSION_TAG: ${{ needs.metadata.outputs.version_tag }}

      - name: Activate stable release
        if: ${{ needs.metadata.outputs.github_release_enabled == 'true' && inputs.promote }}
        env:
          RELEASE_MANIFEST_DIR: ${{ runner.temp }}/release-platform-manifests
          RELEASE_METADATA_PATH: ${{ runner.temp }}/release-metadata/metadata.json
          RELEASE_OUTPUTS_PATH: ${{ runner.temp }}/release-activation/outputs.json
          RELEASE_REPOSITORY: ${{ github.repository }}
          RELEASE_STORAGE_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_RELEASES_AK }}
          RELEASE_STORAGE_BUCKET: ${{ secrets.CLOUDFLARE_R2_RELEASES_BUCKET }}
          RELEASE_STORAGE_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_RELEASES_URL }}
          RELEASE_STORAGE_REGION: auto
          RELEASE_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_RELEASES_SK }}
        run: pnpm exec tools-release activate-stable-release

      - name: Cleanup release + tag on failure
        if: ${{ failure() && needs.metadata.outputs.github_release_enabled == 'true' && steps.create_release.outcome == 'success' }}
        run: |
          set +e
          echo "publish failed after release was created; rolling back release and tag"
          gh release delete "$VERSION_TAG" --cleanup-tag --yes
          # belt-and-suspenders: ensure remote tag is gone even if --cleanup-tag missed
          git push origin --delete "refs/tags/$VERSION_TAG" || true

      - name: Cleanup workflow artifacts
        if: ${{ success() }}
        run: bash .github/scripts/release/github/cleanup-artifacts.sh

  cleanup_partial_release_assets:
    name: Cleanup unpublished release asset artifacts
    needs:
      - metadata
      - build_mac
      - build_mac_intel
      - build_win
      - publish
    if: ${{ always() && needs.metadata.outputs.dry_run != 'true' && needs.publish.result != 'success' }}
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.ref }}

      - name: Delete unpublished release asset artifacts
        env:
          ARTIFACT_CLEANUP_DESCRIPTION: intermediate release asset Actions artifacts from this unpublished run. Canonical manual downloads are only the R2 links in a successful publish summary
          ARTIFACT_NAME_REGEX: "-release-assets$"
        run: bash .github/scripts/release/github/cleanup-artifacts.sh
